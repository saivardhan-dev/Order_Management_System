server:
  port: 8081

spring:
  application:
    name: product-service
    mongodb:
      uri: mongodb://localhost:27017/product_db

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus